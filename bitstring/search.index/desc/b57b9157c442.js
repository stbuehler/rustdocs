rd_("Dc<code>BigEndianBitString</code> functions for specific integer typeBaLength of the bit string in bits.AoGet the <code>ndx</code>th bit.0ChTreat bit string as integer, where bit 0 is the most \xe2\x80\xa6BiCurrent length of the bit string in bits.Cllength of bit string (same as <code>BitString::len()</code>)CnCreate new dynamic-length bit string from fixed bit string \xe2\x80\xa6CcSet the <code>ndx</code>th bit to <code>bit</code>.077DjIterator to iterate over all <code>FixedBitString</code> values with a \xe2\x80\xa6Akget read access to the bitsBamaximum number of bits in storageBgSet current length to <code>len</code>.BaFlips the <code>ndx</code>th bit.00AoReturns the argument unchanged.0BaCalls <code>U::from(self)</code>.0DaIterate through all bit strings until <code>inc</code> overflows.0Cninteger with single bit set. bit 0 is the highest bit (big \xe2\x80\xa6000BdCreate a new zero-length bit string.Dc<code>BigEndianBitString</code> functions for specific integer typeBoutils to support implementing bit string traitsmAppend a bit.AoGet the <code>ndx</code>th bit.>9Dgincrement from right; don\xe2\x80\x99t touch first <code>prefix</code> bits; \xe2\x80\xa6Dhcheck whether another bit string <code>other</code> shares the first \xe2\x80\xa6Eccheck whether another bit string <code>bits</code> is prefixed by <code>self</code>CiA bit string with variable (but possibly limited) length.Cmbigendian utils to support implementing bit string traits \xe2\x80\xa6CnThis crate provides traits to treat various objects as bit \xe2\x80\xa666665555CcSet the <code>ndx</code>th bit to <code>bit</code>.000BaFlips the <code>ndx</code>th bit.000B`Partial ordering on bit strings.0999988882222Ahbits in a single element0002222CbWhether all bits in [ndx..] are <code>true</code>.DeWhether all bits from [ndx..] are <code>true</code> (<code>1</code>).CfNew bit string with all bits set to <code>true</code>.CcWhether all bits in [ndx..] are <code>false</code>.DfWhether all bits from [ndx..] are <code>false</code> (<code>0</code>).CgNew bit string with all bits set to <code>false</code>.BmSet all bits in [ndx..] to <code>true</code>.D`Set all bits from [ndx..] to <code>true</code> (<code>1</code>).BiLongest shared prefix of two bit strings.0AoA bit string with fixed length.Dhcheck whether another bit string <code>value</code> shares the first \xe2\x80\xa6BnSet all bits in [ndx..] to <code>false</code>.DaSet all bits from [ndx..] to <code>false</code> (<code>0</code>).Dhcheck whether another bit string <code>other</code> shares the first \xe2\x80\xa6000EfExtend a <code>FixedBitString</code> to a <code>BitString</code> by also storing a \xe2\x80\xa61111Dfcontains extra types related to the <code>FixedBitString</code> trait.Dgincrement from right; don\xe2\x80\x99t touch first <code>prefix</code> bits; \xe2\x80\xa6000CcSet the <code>ndx</code>th bit to <code>bit</code>.000BfLexicographic ordering on bit strings.0BaFlips the <code>ndx</code>th bit.000CgLength of the longest shared prefix of two bit strings.0000ChGeneric helper methods to treat unsigned integer and \xe2\x80\xa6DeWhether all bits from [ndx..] are <code>true</code> (<code>1</code>).000DfWhether all bits from [ndx..] are <code>false</code> (<code>0</code>).000D`Set all bits from [ndx..] to <code>true</code> (<code>1</code>).0002222<<<<11110000<<<<444444440000<<<<")