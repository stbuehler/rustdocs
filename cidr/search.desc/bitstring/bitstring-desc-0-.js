searchState.loadedDescShard("bitstring", 0, "This crate provides traits to treat various objects as bit …\nExtend a <code>FixedBitString</code> to a <code>BitString</code> by also storing a …\ncontains everything related to the <code>BitString</code> trait.\nget read access to the bits\ncheck whether another bit string <code>bits</code> is prefixed by <code>self</code>\ncontains everything related to the <code>FixedBitString</code> trait.\nReturns the argument unchanged.\nCalls <code>U::from(self)</code>.\nlength of bit string (same as <code>BitString::len()</code>)\nCreate new dynamic-length bit string from fixed bit string …\nutils to support implementing bit string traits\nA bit string with variable (but possibly limited) length.\nAppend a bit.\nSet current length to <code>len</code>.\nFlips the <code>ndx</code>th bit.\nGet the <code>ndx</code>th bit.\nCurrent length of the bit string in bits.\nLexicographic ordering on bit strings.\nLexicographic ordering on bit strings.\nCreate a new zero-length bit string.\nSet the <code>ndx</code>th bit to <code>bit</code>.\nLongest shared prefix of two bit strings.\nLongest shared prefix of two bit strings.\nLength of the longest shared prefix of two bit strings.\nLength of the longest shared prefix of two bit strings.\nPartial ordering on bit strings.\nPartial ordering on bit strings.\nA bit string with fixed length.\nIterator to iterate over all <code>FixedBitString</code> values with a …\ncheck whether another bit string <code>other</code> shares the first …\nFlips the <code>ndx</code>th bit.\nFlips the <code>ndx</code>th bit.\nReturns the argument unchanged.\nGet the <code>ndx</code>th bit.\nTreat bit string as integer, where bit 0 is the most …\nCalls <code>U::from(self)</code>.\nWhether all bits in [ndx..] are <code>false</code>.\nWhether all bits in [ndx..] are <code>true</code>.\nIterate through all bit strings until <code>inc</code> overflows.\nIterate through all bit strings until <code>inc</code> overflows.\nLength of the bit string in bits.\nNew bit string with all bits set to <code>false</code>.\nNew bit string with all bits set to <code>true</code>.\nSet the <code>ndx</code>th bit to <code>false</code>.\nSet the <code>ndx</code>th bit to <code>false</code>.\nSet the <code>ndx</code>th bit to <code>true</code>.\nSet the <code>ndx</code>th bit to <code>true</code>.\nSet the <code>ndx</code>th bit to <code>bit</code>.\nSet all bits in [ndx..] to <code>false</code>.\nSet all bits in [ndx..] to <code>true</code>.\nLength of the longest shared prefix of two bit strings.\nLength of the longest shared prefix of two bit strings.\nGeneric helper methods to treat [u*]-slices as big endian …\ncheck whether another bit string <code>other</code> shares the first …\nbits in a single element\nbits in a single element\nFlips the <code>ndx</code>th bit.\nGet the <code>ndx</code>th bit.\nincrement from right; don’t touch first <code>prefix</code> bits; …\nWhether all bits from [ndx..] are <code>false</code>.\nWhether all bits from [ndx..] are <code>true</code>.\ninteger with single bit set. bit 0 is the highest bit (big …\nSet the <code>ndx</code>th bit to <code>false</code>.\nSet the <code>ndx</code>th bit to <code>true</code>.\nSet the <code>ndx</code>th bit to <code>bit</code>.\nSet the <code>ndx</code>th bit to <code>bit</code>.\nSet all bits from [ndx..] to <code>false</code>.\nSet all bits from [ndx..] to <code>true</code>.\nLength of the longest shared prefix of two bit strings.")